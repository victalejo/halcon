# Web layer dependencies (in addition to requirements.txt).
# Install both:  pip install -r requirements.txt -r requirements-web.txt
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
python-multipart>=0.0.9
